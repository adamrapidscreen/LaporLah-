# E2-S2: Build Desktop Top Navigation Bar

## Description
Create a responsive top navigation bar for desktop viewports (≥768px). Features a sticky glassmorphism header with the LaporLah logo on the left, navigation links centered, and notification bell + user avatar on the right.

## Dependencies
- Stories: E1-S5
- Files: `src/app/layout.tsx`, `src/styles/globals.css`, `src/lib/utils/cn.ts`

## Tasks
1. `src/components/layout/top-nav.tsx`: Create Client Component with sticky positioning, glassmorphism backdrop, logo, centered nav links (Home, Lapor, Followed), right-side notification bell + avatar dropdown
2. `src/components/layout/theme-toggle.tsx`: Create Client Component with theme toggle button (sun/moon icon) using `next-themes` `useTheme` hook

## Implementation Notes

### Top Navigation (`src/components/layout/top-nav.tsx`)
```typescript
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { Bell, Plus } from 'lucide-react';

import { Button } from '@/components/ui/button';
import { Avatar, AvatarFallback, AvatarImage } from '@/components/ui/avatar';
import { cn } from '@/lib/utils/cn';

import { ThemeToggle } from './theme-toggle';

export function TopNav() {
  const pathname = usePathname();

  const navLinks = [
    { href: '/', label: 'Home' },
    { href: '/followed', label: 'Followed' },
  ];

  return (
    <header className="sticky top-0 z-50 w-full border-b border-border/50 backdrop-blur-xl bg-background/80">
      <div className="mx-auto flex h-14 max-w-5xl items-center justify-between px-4">
        {/* Logo */}
        <Link href="/" className="flex items-center gap-2 font-bold text-lg">
          <span className="text-primary">Lapor</span>
          <span>Lah!</span>
        </Link>

        {/* Center Nav */}
        <nav className="flex items-center gap-6">
          {navLinks.map((link) => (
            <Link
              key={link.href}
              href={link.href}
              className={cn(
                'text-sm font-medium transition-colors hover:text-primary',
                pathname === link.href ? 'text-primary' : 'text-muted-foreground'
              )}
            >
              {link.label}
            </Link>
          ))}
          <Button asChild size="sm" className="gap-1.5">
            <Link href="/report/new">
              <Plus className="h-4 w-4" />
              Lapor
            </Link>
          </Button>
        </nav>

        {/* Right Actions */}
        <div className="flex items-center gap-2">
          <ThemeToggle />
          <Button variant="ghost" size="icon" asChild>
            <Link href="/notifications">
              <Bell className="h-5 w-5" />
            </Link>
          </Button>
          <Avatar className="h-8 w-8">
            <AvatarImage src="" alt="User" />
            <AvatarFallback>U</AvatarFallback>
          </Avatar>
        </div>
      </div>
    </header>
  );
}
```

### Theme Toggle (`src/components/layout/theme-toggle.tsx`)
```typescript
'use client';

import { Moon, Sun } from 'lucide-react';
import { useTheme } from 'next-themes';

import { Button } from '@/components/ui/button';

export function ThemeToggle() {
  const { setTheme, theme } = useTheme();

  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}
    >
      <Sun className="h-5 w-5 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
      <Moon className="absolute h-5 w-5 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
      <span className="sr-only">Toggle theme</span>
    </Button>
  );
}
```

### Design Specifications
- **Glassmorphism**: `backdrop-blur-xl bg-background/80 border-b border-border/50`
- **Sticky**: `sticky top-0 z-50`
- **Height**: `h-14` (56px)
- **Max width**: `max-w-5xl mx-auto`
- **Logo**: Bold text, "Lapor" in `text-primary`
- **Active link**: `text-primary`
- **Inactive link**: `text-muted-foreground`
- **Lapor button**: Primary variant with Plus icon

## Acceptance Criteria
- [ ] Top nav renders with sticky positioning at top of viewport
- [ ] Glassmorphism styling: `backdrop-blur-xl bg-background/80`
- [ ] Logo "LaporLah!" on the left with "Lapor" in primary color
- [ ] Nav links centered: Home, Followed, and Lapor button
- [ ] Active link highlighted with `text-primary`
- [ ] Right side: theme toggle, notification bell, user avatar
- [ ] Theme toggle switches between dark/light modes
- [ ] Height is 56px (`h-14`)
- [ ] Max content width constrained to `max-w-5xl`
- [ ] Component uses `'use client'` directive
- [ ] Named exports: `TopNav`, `ThemeToggle`
